#!/bin/bash
# Переключиться обратно на PostgreSQL для разработки
echo "🔄 Переключаюсь на PostgreSQL (development)..."
sed -i 's/^DB_CONNECTION=.*/DB_CONNECTION=pgsql/' .env
php artisan config:clear
php artisan cache:clear
echo "✅ Теперь используется PostgreSQL"
echo "💡 Режим разработки - можно изменять данные"
echo ""
echo "Для работы с копией MySQL: ./artisan-use-mysql"
echo "Для просмотра production MySQL (read-only): ./artisan-use-mysql-prod"
