#!/bin/bash
# Переключиться на копию MySQL armbase-2 (read-write)
echo "🔄 Переключаюсь на MySQL Dev (armbase-2 копия)..."
sed -i 's/^DB_CONNECTION=.*/DB_CONNECTION=mysql_dev/' .env
php artisan config:clear
php artisan cache:clear
echo "✅ Теперь используется MySQL Dev (armbase-2)"
echo "💾 Режим READ-WRITE - полный доступ к копии базы данных"
echo ""
echo "Для возврата на PostgreSQL: ./artisan-use-postgres"
echo "Для production MySQL (read-only): ./artisan-use-mysql-prod"
