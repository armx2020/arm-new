#!/bin/bash
# Переключиться на production MySQL (read-only)
echo "🔄 Переключаюсь на Production MySQL (read-only)..."
sed -i 's/^DB_CONNECTION=.*/DB_CONNECTION=mysql_production/' .env
php artisan config:clear
php artisan cache:clear
echo "✅ Теперь используется Production MySQL"
echo "⚠️  Режим READ-ONLY - изменения невозможны"
echo ""
echo "Для возврата на PostgreSQL: ./artisan-use-postgres"
echo "Для dev копии MySQL: ./artisan-use-mysql-dev"
